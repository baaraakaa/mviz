import { ForceGraphMethods, NodeObject } from "react-force-graph-3d"

type eProps = {
    node: NodeObject,
    fgRef: ForceGraphMethods
}

export default function EditPanel({node, fgRef}: eProps){
    return <div>

    </div>
}